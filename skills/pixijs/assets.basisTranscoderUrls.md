# Variable: basisTranscoderUrls

> `const` **basisTranscoderUrls**: `object`

**`Advanced`**

The urls for the Basis transcoder files.
These can be set to custom paths if needed.

## Type Declaration

### jsUrl

> **jsUrl**: `string` = `'https://cdn.jsdelivr.net/npm/pixi.js/transcoders/basis/basis_transcoder.js'`

### wasmUrl

> **wasmUrl**: `string` = `'https://cdn.jsdelivr.net/npm/pixi.js/transcoders/basis/basis_transcoder.wasm'`
