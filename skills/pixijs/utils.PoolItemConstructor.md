# Type Alias: PoolItemConstructor()\<K\>

> **PoolItemConstructor**\<`K`\> = () => `K`

**`Advanced`**

The constructor of an object that can be stored in a [Pool](utils.Pool.md).

## Type Parameters

### K

`K` *extends* [`PoolItem`](utils.PoolItem.md)

The type of the object that can be stored in a [Pool](utils.Pool.md).

## Returns

`K`
