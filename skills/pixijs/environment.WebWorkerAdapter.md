# Variable: WebWorkerAdapter

> `const` **WebWorkerAdapter**: [`Adapter`](environment.Adapter.md)

**`Advanced`**

This is an implementation of the [Adapter](environment.Adapter.md) interface.
It can be used to make Pixi work in a Web Worker.
