# Variable: shapeBuilders

> `const` **shapeBuilders**: [`Record`](https://www.typescriptlang.org/docs/handbook/utility-types.html#recordkeys-type)\<`string`, `ShapeBuildCommand`\> = `{}`

**`Advanced`**

A record of shape builders, keyed by shape type.
