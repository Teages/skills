# Variable: ktxTranscoderUrls

> `const` **ktxTranscoderUrls**: `object`

**`Advanced`**

The urls for the KTX transcoder library.
These can be set to custom paths if needed.

## Type Declaration

### jsUrl

> **jsUrl**: `string` = `'https://cdn.jsdelivr.net/npm/pixi.js/transcoders/ktx/libktx.js'`

### wasmUrl

> **wasmUrl**: `string` = `'https://cdn.jsdelivr.net/npm/pixi.js/transcoders/ktx/libktx.wasm'`
